@startuml

abstract class handle {
   -- Events --
   ObjectBeingDestroyed
}


enum matdoc.Enums.AccessLevel {
   -- Enumeration Values --
   Public
   Protected
   Private
   -- Methods --
   .. Hidden ..
   {method} + varargout = colon(varargin)
   .. Public ..
   {method} + e = AccessLevel(varargin)
   {method} + lhs1 = char(rhs1)
   {method} + isEqual = eq(this, other)
   {method} + isEqual = ne(this, other)
   .. Static ..
   {method}{static} + enum = from(val)
   {method}{static} + enum = fromNum(val)
   {method}{static} + enum = fromStr(val)
   .. Static Hidden ..
   {method}{static} + E = empty(varargin)
}

matdoc.Enums.AccessLevel --|> uint32

class matdoc.Meta.Class {
   -- Properties --
   .. Protected ..
   {field} # Configuration < matdoc.Meta.Super.Meta
             The handle to the export configuration

Help for matdoc.Meta.Class/Configuration is inherited from superclass MATDOC.META.SUPER.META
             
   {field} # Parent < matdoc.Meta.Super.Meta
             the parent object

Help for matdoc.Meta.Class/Parent is inherited from superclass MATDOC.META.SUPER.META
             
   {field} # UmlRoot < matdoc.Meta.Super.Meta
             Handle to the parent matdoc.Documentation class

Help for matdoc.Meta.Class/UmlRoot is inherited from superclass MATDOC.META.SUPER.META
             
   .. SetAccess = Protected ..
   {field} + Abstract
             Is the class abstract?
             
   {field} + ConstructOnLoad
             Construct OnLoad
             
   {field} + ContainingPackage
             ContainingPackage
             
   {field} + Description
             Class description
             
   {field} + DetailedDescription
             Detailed class description
             
   {field} + EnumerationMemberList
             The list of EnumerationMembers
             
   {field} + EventList
             The list of events
             
   {field} + HandleCompatible
             Is the class a handle class?
             
   {field} + Hidden
             Is the class hidden?
             
   {field} + InferiorClasses
             InferiorClasses
             
   {field} + InheritationRelations
             The inheritation relation ships
             
   {field} + isBuiltIn
             If true the wrapped class is a built in matlab class
             
   {field} + metaObj < matdoc.Meta.Super.Meta
             the actual wrapped meta object (meta.package, meta.class,
  meta.method, meta.property, etc.)

Help for matdoc.Meta.Class/metaObj is inherited from superclass MATDOC.META.SUPER.META
             
   {field} + MethodList
             The list of methods
             
   {field} + MethodListFlattend
             A complete list of methods incl the ones inheritated from
  other classes
             
   {field} + Name
             The name of Class
             
   {field} + plantUML < matdoc.Super.DisplayUML
             The plantUML representation of the object

Help for matdoc.Meta.Class/plantUML is inherited from superclass MATDOC.SUPER.DISPLAYUML
             
   {field} + PropertyList
             The list of properties
             
   {field} + PropertyListFlattend
             A complete list of properties incl the ones inheritated from
  other classes
             
   {field} + Sealed
             Is the class sealed?
             
   {field} + SortedMethodList
             It cotains a Nx2 cell array. The first column holds the
  category (categoryUML property) and the second column the
  actual cell array of handles to the meta classes.
  N = number of unique categories.
             
   {field} + SortedPropertyList
             It cotains a Nx2 cell array. The first column holds the
  category (categoryUML property) and the second column the
  actual cell array of handles to the meta classes.
  N = number of unique categories.
             
   {field} + SuperclassList
             The list of super classes
             
   {field} + SuperclassListFlattend
             A complete list of super classes incl. nested inheritated classes
             
   -- Methods --
   .. Protected ..
   {method} # umlStr = getPlantUML(this)
   {method} # umlStr = getPlantUmlEnumerationValues(this)
   {method} # umlStr = getPlantUmlEvents(this)
   {method} # umlStr = getPlantUmlInheritanceRelation(this)
   {method} # umlStr = getPlantUmlMethods(this)
   {method} # umlStr = getPlantUmlProperties(this)
   {method} # getSortedMethodList(this)
   {method} # getSortedPropertyList(this)
   {method} # val = getSuperclassListFlattend(this)
   {method} # walkMeta(this)
   .. Public ..
   {method} + this = Class(metaObj, parent)
   {method} + is_eq = eq(this, other) < matdoc.Meta.Super.Meta
   {method} + is_ne = ne(this, other) < matdoc.Meta.Super.Meta
   {method} + [B, IX] = sort(this, dim, mode) < matdoc.Meta.Super.Meta
   .. Static Hidden ..
   {method}{static} + E = empty(varargin)
   -- Events --
   ObjectBeingDestroyed
}

matdoc.Meta.Class --|> matdoc.Meta.Super.Meta

abstract class matdoc.Meta.Super.Meta {
   -- Properties --
   .. Abstract  SetAccess = Protected ..
   {field}{abstract} + Name
                       The name of the meta object
                       
   .. Protected ..
   {field} # Configuration
             The handle to the export configuration
             
   {field} # Parent
             the parent object
             
   {field} # UmlRoot
             Handle to the parent matdoc.Documentation class
             
   .. SetAccess = Protected ..
   {field} + metaObj
             the actual wrapped meta object (meta.package, meta.class,
  meta.method, meta.property, etc.)
             
   {field} + plantUML < matdoc.Super.DisplayUML
             The plantUML representation of the object

Help for matdoc.Meta.Super.Meta/plantUML is inherited from superclass MATDOC.SUPER.DISPLAYUML
             
   -- Methods --
   .. Abstract Protected ..
   {method}{abstract} # getPlantUML(this) < matdoc.Super.DisplayUML
   {method}{abstract} # walkMeta(this)
   .. Public ..
   {method} + is_eq = eq(this, other)
   {method} + this = Meta(metaObj, parent)
   {method} + is_ne = ne(this, other)
   {method} + [B, IX] = sort(this, dim, mode)
   .. Static Hidden ..
   {method}{static} + E = empty(varargin)
   -- Events --
   ObjectBeingDestroyed
}

matdoc.Meta.Super.Meta --|> matdoc.Super.DisplayUML

abstract class matdoc.Super.DisplayUML {
   -- Properties --
   .. SetAccess = Protected ..
   {field} + plantUML
             The plantUML representation of the object
             
   -- Methods --
   .. Abstract Protected ..
   {method}{abstract} # getPlantUML(this)
   .. Public ..
   {method} + this = DisplayUML()
   .. Static Hidden ..
   {method}{static} + E = empty(varargin)
   -- Events --
   ObjectBeingDestroyed
}

matdoc.Super.DisplayUML --|> handle

class uint32 {
}


@enduml