classdef (Abstract) WithSphinxDoc < handle
   
    %% METHODS: ABSTRACT
    methods (Abstract)
        
        %% - sphinxDocStr = getSphinxDoc(this, ident_)
        sphinxDocStr = getSphinxDoc(this, ident_)
        
    end % methods
    
end % classdef WithSphinxDoc < handle
