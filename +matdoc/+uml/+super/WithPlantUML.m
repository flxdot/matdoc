classdef (Abstract) WithPlantUML < handle
   
    %% METHODS: ABSTRACT
    methods (Abstract)
        
        %% - umlStr = getPlantUML(this, ident_)
        umlStr = getPlantUML(this, ident_);
        
    end % methods
    
end % classdef WithPlantUML < handle
